= form_for @metric do |f|
  %fieldset
    - if @metric.errors.any?
      #error_explanation
        %h2= "#{pluralize(@metric.errors.count, "error")} prohibited this metric from being saved:"
        %ul
          - @metric.errors.full_messages.each do |msg|
            %li= msg

    .field
      = f.label :measure
      = f.text_area :measure, rows: "5"
    .checkbox
      = f.check_box :active
      = f.label :active
      
    .actions
      = f.submit 'Save', class: :submit_button

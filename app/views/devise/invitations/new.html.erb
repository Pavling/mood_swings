<h1><%= t "devise.invitations.new.header" %></h1>

<%= form_for resource, :as => resource_name, :url => invitation_path(resource_name), :html => {:method => :post} do |f| %>
  <%= devise_error_messages! %>

  <fieldset>
  <% resource.class.invite_key_fields.each do |field| -%>
    <div class='field'>
      <%= f.label field %>
      <%= f.text_field field %>
    </div>
  <% end -%>

<<<<<<< HEAD
  <div class='field'>
    <%= f.label :cohort_id, 'Cohort' %>
    <%= f.collection_select :cohort_id, @cohorts, :id, :name, prompt: 'Please choose, or leave blank for cohort admin' %>
=======
  <div>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.label :cohort_id, 'Cohort' %><br />
    <%= f.collection_select :cohort_id, @cohorts, :id, :name, prompt: 'Please choose' %>
>>>>>>> upstream/master
  </div>

  <div class='field'>
    <%= f.submit t("devise.invitations.new.submit_button"), class: :submit_button %>
  </div>

</fieldset>
<% end %>

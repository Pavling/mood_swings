%h1 Listing users

%table#mobile.responsive
  %tr#administering
    %td Admin

%table.responsive
  %tr
    %th Name
<<<<<<< HEAD
    %th.hide Cohort
    %th.hide Administering
    %th.hide Manage

  - @users.each do |user|
    %tr{:class => ('admin' if user.administered_cohorts.map(&:name).any?)}
      %td= link_to user.email, user
      %td= user.cohort.try(:name)
      %td= user.administered_cohorts.map(&:name).to_sentence
      %td.hide
        = link_to 'Show', user
=======
    %th Email
    %th Skip Reminders?
    %th Cohort
    %th Administering
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.name
      %td= user.email
      %td= user.skip_email_reminders
      %td= user.cohort.try(:name)
      %td= user.administered_cohorts.map(&:name).to_sentence
      %td= link_to 'Show', user if can? :show, user
      %td= link_to 'Edit', edit_user_path(user) if can? :update, user
>>>>>>> upstream/master

%br

= link_to 'Invite User', new_user_invitation_path, class: :submit_button
